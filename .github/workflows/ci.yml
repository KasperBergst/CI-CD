name: Integrate with master
on:
  push:
    branches:
      - test
      
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Push to master
        run: |
          git checkout test
          git rebase origin/master
          git push origin master
